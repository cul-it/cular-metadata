{
    "DEPOSITOR/COLLECTION": {
        "phys_coll_id": "bib|archival|other (required if available)",
        "number_files": "int (required if available)",
        "steward": "netID (required if available)",
        "awslocation": {
            "s3://BUCKET/COLL": {
                "date_ingest": "YYYY-MM-DD (auto-generated)",
                "date_update": "YYYY-MM-DD (auto-generated)"
            }
        },
        "items": {
            "SUBDIR_NAME": {
                "sfslocation": "SFSpath",
                "FILENAME" : {
                    "bib_id": "bibliographic record id (required if available)",
                    "sha1": "hash (required)",
                    "md5": "hash (optional)",
                    "size": "int (required)"
                }
            }
        }
    }
}
